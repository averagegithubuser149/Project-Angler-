animationStarted = false;
currentFrame = 0;

// "obj_player" Item Detection
var currentObject = object_index;
 {
    // Check if the object is not the current object
    if (object_index != currentObject) {
        // Perform actions on other objects
        // ...
    }
}

if place_meeting(x, y, object_index)
{
	
    // Collision occurred between obj_enemy and obj_player
    // Implement collision reaction here
}