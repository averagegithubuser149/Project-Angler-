
var decreaseSpeed = false;  // Flag to indicate if speed decrease is active
var decreaseTimer = 60;     // Timer duration in frames (adjust as needed)



 if other == obj_player
 {
    // Trigger animation [should be anyways, I'll comeback to this later]
    image_speed = 1;
    image_index = 1;
    
    // Set the speed decrease
    decreaseSpeed = true;
    decreaseTimer = 60;  // Reset the timer duration (adjust as needed)
}

