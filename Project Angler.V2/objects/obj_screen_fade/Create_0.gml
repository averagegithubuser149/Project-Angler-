// Create Event of obj_screen_fade

alpha = 0; // Start with fully transparent (black screen)

fadeSpeed = 0.10; // Adjust the speed of the fade (larger values make it faster)

// Set the position to the center of the room
x = room_width / 2;
y = room_height / 2;
